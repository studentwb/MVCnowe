@model IEnumerable<User> 
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">User List</h2>
        </div>
        <div class="col=6 text-end">
            <a asp-controller="User" asp-action="CreateUser" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>  &nbsp; Create New User
            </a>
        </div>
    </div>


<table class="table table-bordered tabel-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Surename
            </th>
            <th>
                Email
            </th>
            <th>
                Age
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td withd="50%">
                    @obj.Name
                </td>
                <td width="30%">
                    @obj.Surename
                </td>
                 <td width="30%">
                    @obj.Email
                </td>
                <td width="30%">
                    @obj.Age
                </td>
                
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="User" asp-action="EditUser" asp-route-id="@obj.Id"><i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="User" asp-action="DeleteUser" asp-route-id="@obj.Id"><i class="bi bi-trash-fill"></i> Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>